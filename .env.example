# 复制为 .env.local 并填入真实值（.env.local 不会提交 git）

# SysBuilder 后端地址
SYSBUILDER_BACKEND_URL=http://47.79.4.19

# API Token（可选）— 在 SysBuilder 设置页生成，格式 sb_pat_xxxxx
# 不填则自动使用 Electron 登录后写入的 session cookie（~/.copilark/bridge-auth.txt）
SYSBUILDER_TOKEN=
